data_util.py 功能为训练数据数据预处理，分词，词性标注，依存句法分析，根据是否为情感词、主题词打标签打标签，最终输出文件为crf模型规定格式的输入文件。
将上面输出的文件输入到crf模型中训练模型model。
将测试数据放入data_util.py中进行分词和提取特征，输出文件。
将上面输出的文件输入到训练好的模型model中，得到结果文件result。
match.py 功能为根据result文件进行标注序列配对，找到情感词和主题词、极性对，并输出最后结果。